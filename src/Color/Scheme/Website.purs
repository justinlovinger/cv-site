module Color.Scheme.Website where

import CSS.Color (Color, black, gray, white)

background ∷ Color
background = white

foreground ∷ Color
foreground = black

altForeground ∷ Color
altForeground = gray
